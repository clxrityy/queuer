[project]
name = "QuestionQueuer"
description = "A Discord bot for managing queues for on-interval events."
readme = "README.md"
requires-python = ">=3.11"
license-files = ["LICENSE"]
version = "0.1.0"