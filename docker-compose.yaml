services:
  question-queuer:
    image: queuer:latest
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - BOT_CLIENT_ID=${BOT_CLIENT_ID}
      - BOT_APPLICATION_ID=${BOT_APPLICATION_ID}
      - GUILD_ID=${GUILD_ID}
      - ENV=${ENV:-development}
      - DATABASE_PATH=${DATABASE_PATH:-data/queuer.sqlite3}
      - DEFAULT_TIMEZONE=${DEFAULT_TIMEZONE:-UTC}
    volumes:
      - ./data:/bot/data
    restart: unless-stopped